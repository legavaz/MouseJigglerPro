<Window x:Class="MouseJigglerPro.MVVM.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MouseJigglerPro"
        xmlns:viewModel="clr-namespace:MouseJigglerPro.MVVM.ViewModel"
        mc:Ignorable="d"
        Title="Mouse Jiggler Pro" Height="200" Width="350"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize"
        Icon="/Assets/Icons/default.ico">

    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding StatusText}"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="DarkSlateGray"/>

        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Jiggle mode" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="16"/>
            <ToggleButton Grid.Column="1" IsChecked="{Binding IsJigglingActive, Mode=TwoWay}" VerticalAlignment="Center">
                <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="border" BorderBrush="LightGray" BorderThickness="1" CornerRadius="15" Width="60" Height="30" Background="LightGray">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="1" Color="Gray" Opacity="0.3" BlurRadius="5"/>
                                </Border.Effect>
                            </Border>
                            <Ellipse x:Name="dot" Fill="White" Width="24" Height="24" HorizontalAlignment="Left" Margin="3,0,0,0">
                                <Ellipse.Effect>
                                    <DropShadowEffect ShadowDepth="1" Color="Gray" Opacity="0.5" BlurRadius="3"/>
                                </Ellipse.Effect>
                            </Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4CD964"/>
                                <Setter TargetName="dot" Property="HorizontalAlignment" Value="Right"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </ToggleButton.Template>
            </ToggleButton>
        </Grid>

        <!-- Placeholder for Settings button/link -->
        <TextBlock Grid.Row="2" Margin="0,0,0,10" HorizontalAlignment="Center">
            <Hyperlink Command="{Binding OpenSettingsCommand}">Настройки</Hyperlink>
        </TextBlock>

    </Grid>
</Window>